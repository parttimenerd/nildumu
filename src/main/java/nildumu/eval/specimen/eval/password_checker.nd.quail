// Typical password checker adaption, that compares with "1", as not all tools support
// low inputs
secret int32 h;
public int32 l;
observable int32 o;
if (h == l) then
    assign o := 1;
else
    assign o := 0;
fi